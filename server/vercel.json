{
  "version": 2,
  "installCommand": "cd .. && pnpm install --frozen-lockfile --ignore-scripts",
  "functions": {
    "api/index.ts": {
      "includeFiles": "public/**"
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
